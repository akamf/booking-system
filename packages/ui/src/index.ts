/**
 * @booking/ui
 *
 * Web-only shared components built on shadcn/ui + Tailwind, consumed by
 * apps/web-admin and apps/web-public. Mobile does NOT consume this
 * package — see DECISIONS.md ADR-0007.
 *
 * Filled out in Phase 3 once we know which shared components actually
 * appear in both web surfaces.
 */
export {};
